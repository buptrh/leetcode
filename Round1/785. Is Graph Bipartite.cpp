class Solution {
public:

    bool isBipartite(vector<vector<int>>& graph) {
        unordered_set<int> A;
        unordered_set<int> B;
        if(graph.size() == 0 || graph[0].size() == 0) {
        	return true;
        }
        for(vector<int> edge : graph) {

        }
    }
};