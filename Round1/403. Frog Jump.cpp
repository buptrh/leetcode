class Solution {
public:
    bool canCross(vector<int>& stones) {
    	vector<set<int>> dp(stones.size(), set<int>());

        for(int i = 0; i < stones.size(); i++) {
        	for(int j = 0; j < i; j++) {
        		
        	}
        }
    }
};